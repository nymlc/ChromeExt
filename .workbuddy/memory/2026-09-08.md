# 2026-09-08

## 新增 JSON 格式化模块（popup 端工具）
- 新建 `popup/modules/JsonFormatter.js`（继承 BaseModule，模块名 `jsonFormatter`）：格式化（2/4 空格/Tab）、压缩、转义/去转义（含 `\uXXXX` 与宽松反斜杠还原）、按键排序（递归+数字感知）、示例、清空、复制、下载 `formatted.json`；输入防抖 300ms 实时校验，粘贴后自动格式化。
- 自研极简 tokenizer 做语法高亮（先 HTML 转义再套 span，避免注入）；解析失败展示行列并把光标 setSelectionRange 跳到出错位置（V8 新消息自带 line/column 时不重复追加）。
- 接入点：`popup.html`（模块卡片 + `jsonFormatterModuleContent` 子页容器 + script 标签）、`popup.js`（contentMap / 初始化 / bindEntry）、`shared/dataKeys.js`（新增 jsonFormatter 4 个 key，导出导入自动覆盖）、`popup/popup.css`（`.jf-*` 样式）、README。
- 子页面内容超 492px 上限：用 `.scrollable-area` 包一层让内容区整体滚动，输出框固定 148px 内滚。
- 验证方式（无 node_modules、jsdom 装不上）：`node --check` 语法 + Node 里用 DOM stub（getElementById 返回假元素）跑完整交互链路。踩坑：Node 21+ 的 `global.navigator` 是只读 getter，测试里要用 `Object.defineProperty` 覆盖。
